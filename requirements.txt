# Runtime dependencies actually imported by the tool.
# Versions verified against Python 3.10-3.13 (all seven resolve on each);
# behaviour re-verified with tests/run_all.sh after every bump.

Flask==3.1.3
Werkzeug==3.1.8         # Flask 3's partner; the old Flask 2.3 + Werkzeug 3 clash is gone
Flask-Cors==6.0.5
Wand==0.7.2             # ImageMagick binding - does the mutations
Pillow==12.3.0          # fallback engine for formats ImageMagick lacks a delegate for
numpy==2.2.6            # used by the fallback implode / rotational-blur / raise
python-dotenv==1.2.3
