# Image-Augmentation
**Image-Augmentation** is a deterministic image augmentation and mutation tool for generating reproducible datasets. It applies configurable transformations and controlled mutations to images, enabling researchers to systematically create augmented datasets while ensuring the same input and configuration produce identical outputs
